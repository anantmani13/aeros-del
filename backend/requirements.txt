# ═══════════════════════════════════════════════════════════════
# SIH — Delhi NCR AQI Forecasting System (backend)
# Python 3.10+ (tested on Python 3.14)
# ═══════════════════════════════════════════════════════════════

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0

# ── HTTP clients ──────────────────────────────────────────────
httpx>=0.27
aiohttp>=3.9

# ── Data stack ────────────────────────────────────────────────
numpy>=1.26
pandas>=2.2
scipy>=1.13
aiosqlite>=0.20
python-dotenv>=1.0
joblib>=1.4

# ── ML stack (optional) ───────────────────────────────────────
# Install for the full trained-prediction pipeline. The system
# degrades gracefully to a statistical baseline without them.
xgboost>=2.0
lightgbm>=4.0
scikit-learn>=1.5
torch>=2.3

# ── NLP (LLM advisories via Gemini; local NLG engine is offline default) ──
# Requires GEMINI_API_KEY for the LLM path. Local NLP engine works offline.
google-genai>=1.0
google-generativeai>=0.7